## Auto-generated by scripts/auto_config.py
## Hardware: NVIDIA GeForce RTX 4080 (16.0 GB VRAM, CUDA 12.4)
## Tier: high | Approach: pose_transformer | Variant: WLASL2000
## Re-run to regenerate, or edit manually.

approach: pose_transformer
wlasl_variant: 2000
# num_classes is auto-derived from wlasl_variant (100 -> 100, 300 -> 300, etc.)
num_keypoints: 543

# Temporal
T: 64

# Features
use_motion: true

# Model architecture
d_model: 384
nhead: 8
num_layers: 6
dropout: 0.5

# Data loading
num_workers: 8

# Training
epochs: 400
batch_size: 64
lr: 3.0e-04
weight_decay: 5.0e-04
warmup_epochs: 15
label_smoothing: 0.0
grad_clip: 1.0
fp16: true
weighted_sampling: true
early_stopping_patience: 50
mixup_alpha: 0.15

# Scheduler
scheduler: cosine

# Evaluation
use_tta: false

# Logging
use_wandb: false
use_tensorboard: true
log_interval: 10

# Inference
confidence_threshold: 0.6
smoothing_window: 5
buffer_size: 64
fps_display: true

# Paths
data_dir: data
output_dir: outputs
checkpoint_dir: checkpoints
log_dir: logs
