## Auto-generated by scripts/auto_config.py
## Hardware: Apple i386 (MPS)
## Tier: mps | Variant: WLASL100
## Re-run to regenerate, or edit manually.

approach: stgcn_proto
wlasl_variant: 100
num_keypoints: 543

# Temporal
T: 64

# Features
use_motion: true

# Model (ST-GCN)
d_model: 128
gcn_channels: [64, 128, 128]
num_layers: 3
dropout: 0.1

# Prototypical training
n_way: 10
k_shot: 3
q_query: 2
num_episodes: 200

# Data loading
num_workers: 0

# Training
epochs: 200
batch_size: 16
lr: 1.0e-03
weight_decay: 1.0e-04
warmup_epochs: 10
grad_clip: 1.0
fp16: false
early_stopping_patience: 30

# Scheduler
scheduler: cosine

# Evaluation
use_tta: false

# Logging
use_wandb: false
use_tensorboard: true
log_interval: 10

# Inference
confidence_threshold: 0.6
smoothing_window: 5
buffer_size: 64
fps_display: true

# Paths
data_dir: data
output_dir: outputs
checkpoint_dir: checkpoints
log_dir: logs
